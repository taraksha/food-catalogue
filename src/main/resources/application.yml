server:
  port: 9096

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8998/eureka/

spring:
  application:
    name: Food-Catalogue-Service
  main:
    banner-mode: off #turn off spring boot banner
  datasource:
    url: jdbc:mysql://localhost:3306/foodcataloguedb
    username: restaurantprjt
    password: restaurantprjt
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update #everytime the server starts it creates new table if in create mode
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect # dialect for converting orm to sql language
